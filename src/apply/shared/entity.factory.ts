export class EntityFactory {
    constructor() {
    }

    // public static create(resourceName: string) {
    //     let className = resourceName.charAt(0).toUpperCase() + resourceName.slice(1);
    // 
    //     return new `${className}`();
    // }
}
